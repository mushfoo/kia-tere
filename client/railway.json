{
  "build": {
    "builder": "NIXPACKS",
    "nixpacksPlan": {
      "providers": ["node"],
      "phases": {
        "install": {
          "cmds": ["npm i --legacy-peer-deps"]
        }
      }
    }
  }
}

{
  "build": {
    "builder": "NIXPACKS",
    "nixpacksPlan": {
      "providers": ["nodejs"],
      "phases": {
        "install": {
          "cmds": ["npm i --legacy-peer-deps"]
        }
      }
    }
  }
}
